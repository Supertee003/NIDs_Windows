# AEGIS NIDs Windows — Integration Baseline (PHASE 0/1 Exit)

**Date:** 2026-09-06
**Baseline tag:** `pre-integration-7889d99` (ก่อน cleanup)
**Current HEAD:** `34776a0` — chore(repo): remove legacy core/core duplicate tree (W2)

## สิ่งที่ทำแล้ว

### P0 Repository Hygiene (commit `4a5191c`)
- untrack `backups/` (105 ไฟล์ LOCAL_BACKUP) — ยังอยู่บนดิสก์
- untrack `reports/` (25 generated compliance JSON) — regenerate ด้วย scripts ได้
- untrack `aegis_restore.ps1.bak.20260905_120855`
- `.gitignore` เพิ่ม `backups/`, `reports/`, `*.bak.*`

### W2 Legacy Audit (commit `34776a0`)
- ลบ `core/core/` (98 ไฟล์) — stale copy ของ `core/` ไม่มี reference ใน build.zig / source / scripts / CI
- `zig build` ผ่านหลังลบ

## Baseline Numbers

| Metric | Value |
|---|---|
| tracked files (core zig) | 228 |
| test files (tests/) | 32 |
| PHASE*_README.md tracked | 20 (จำเป็นต้อง consolidate เป็น canonical doc — G1 ต่อไป) |
| build | PASS (zig build) |
| tests | 2412/2412 assertions PASS, แต่ 3 test steps FAIL |
| working tree | clean |

## Known Issue (environment, ไม่ใช่ regression)

3 test steps fail เพราะ tests log error จาก WFP kernel device:
`\\.\AegisWfpDevice` → `error=0x5` (access denied) — ต้องการ admin privilege /
driver ติดตั้งจึงจะผ่าน ส่งผลกับ `lifecycle`, `rust_pep_integration`,
`policy_plane` test steps

→ G10 (Rust PEP) / G12 (Real WFP) ต้องทดสอบบน elevated shell จริง

## งานถัดไป (ตาม report ลำดับ PHASE)

1. **G1** — consolidate 20 PHASE READMEs → canonical architecture doc 1 ฉบับ
2. **G2** — freeze Canonical Event contract
3. **G3+** — runtime spine / fabric / flow พิสูจน์ golden path

## กฎยึด

- ห้ามเพิ่ม feature ก่อน G1/G2 เสร็จ
- หนึ่ง commit = หนึ่ง engineering idea + หนึ่ง proof
- ตรวจ reference ก่อนลบทุกครั้ง (`git grep`, `git log --all -- <file>`)
